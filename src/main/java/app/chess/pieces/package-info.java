/**
 * Collection of standard chess pieces.
 * <br>
 * For unknown reasons all pieces take `boolean isBlack` as constructor parameter. It would be better to take
 * ChessPieceColor as it would be more semantic and explicit, but at this point it would require too many changes.
 */
package app.chess.pieces;