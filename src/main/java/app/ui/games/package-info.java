/**
 * Game-specific elements of user interface Every game should contain:
 * <ul>
 *     <li> GraphicalPiece which is then displayed on screen </li>
 *     <li> connector (see ChessConnector) that creates new board with appropriate graphical piece supplier </li>
 * </ul>
 */
package app.ui.games;